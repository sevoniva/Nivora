# Nivora Capability Status

This status page is the public source of truth for implemented vs. partial vs. placeholder behavior. Historical audit reports in this directory are useful context, but this page should be treated as the current summary.

Current maturity: **hardened beta-candidate (0.9.0-rc.1), not production-ready**.

Nivora is **not production-ready**. All 11 core runtime stores have PostgreSQL implementations, audit hash chaining is wired through the audit-producing store paths, route/RBAC tests cover critical cases, runner isolation profiles and production config gates exist, and packaging smoke checks cover Helm and Docker Compose profiles. Remaining blockers include OS-level runner sandboxing, live install and restore drills at production scale, complete external adapter integrations, remote MCP controls, tenant-scope proof, and final API/operation hardening. See `STORE_PERSISTENCE_MATRIX.md`, `ENTERPRISE_PRODUCTION_READINESS_REVIEW.md`, `ENTERPRISE_READINESS_MATRIX.md`, and the historical audit reports for evidence and limitations.

## Status Legend

| Status | Meaning |
|---|---|
| implemented | Implemented, tested, and verifiable for the documented local/foundation scope. |
| partial | Useful implementation exists, but important durability, safety, persistence, scale, or integration gaps remain. |
| foundation | Model, ports, adapters, examples, and tests exist to support future hardening, but the capability is not complete. |
| placeholder | Route, package, or document exists mainly to reserve shape and return structured not_implemented or skeleton behavior. |
| experimental | Available only for guarded evaluation or local validation; not production automation. |
| documented-only | Described in docs but not materially implemented. |
| missing | Not implemented and not usable. |

## Capability Matrix

| Capability | Status | Evidence | Notes / Limitations |
|---|---|---|---|
| PipelineRun runtime | implemented | `internal/usecase/pipeline`, `internal/adapters/executor/shell`, `scripts/smoke-pipelinerun-local.sh` | Shell-based local/foundation runtime; not a full distributed workflow engine. |
| Pipeline definition catalog | partial | `internal/usecase/pipeline/catalog.go`, `internal/adapters/repository/postgres/pipeline_definition_store.go`, `000010_catalog_persistence`, `/api/v1/pipelines`, `/api/v1/pipelines/{id}/runs`, `/api/v1/pipelines/{id}/versions`, `nivora pipeline definition`, `docs/dev/pipeline-definitions.md` | Validated Pipeline DSL definitions can be created, listed, updated with version/hash, fetched, disabled, run from the saved catalog definition, and persisted when `database.runtime_store: postgres` is configured. PipelineRuns created from the catalog keep `pipelineId` and `pipelineVersionId`. Full historical version browsing and scheduling policy remain future work. |
| DeploymentRun runtime | partial | `internal/usecase/deployment`, deployment API routes, smoke dry-run scripts, `internal/adapters/repository/postgres/deployment_store.go`, optional PostgreSQL recovery tests in `internal/adapters/repository/postgres/runtime_recovery_integration_test.go` | PostgreSQL persistence and restart-style repository recovery are covered when `NIVORA_RUN_POSTGRES_INTEGRATION=true`; production worker orchestration and full CD hardening remain incomplete. |
| ReleaseExecution | partial | `internal/usecase/releaseorchestration`, release API routes, `POST /api/v1/releases/{id}/cancel`, `POST /api/v1/releases/{id}/evidence`, `nivora release cancel`, `nivora release evidence`, `make verify-release`, `internal/adapters/repository/postgres/release_orchestration_store.go`, optional PostgreSQL recovery tests in `internal/adapters/repository/postgres/runtime_recovery_integration_test.go`, runtime recovery center aggregation | Sequential orchestration, release-level cancel intent, release evidence generation, PostgreSQL persistence, target state recovery, and stale execution queries exist; release cancel does not roll back or cancel existing ReleaseExecutions, and automatic ReleaseExecution mutation in recovery remains manual/report-only. |
| Runner protocol | partial | runner routes in `internal/api/http/routes/routes.go`, PipelineStore runner methods, runner-token scope tests, unrelated job mutation negative tests, `nivora runner offline-detect` | Token/heartbeat/claim/log/status/offline-detect foundations exist and runner tokens are scoped to protocol endpoints; sandboxing and fleet-scale behavior are not production-grade. |
| Shell executor | implemented | `internal/adapters/executor/shell`, 18 shell executor tests | Enterprise controls: timeout clamp (3600s max), output truncation (10MB), workspace isolation with cleanup, sensitive env blocklist (20+ patterns), process group cleanup (Setpgid+SIGKILL), env isolation mode. NOT a container/VM sandbox; operators must still isolate untrusted workloads. |
| Kubernetes YAML apply | experimental | `internal/adapters/executor/yaml_apply`, guarded deployment apply command/API | Apply and rollback require explicit confirmation; no default destructive behavior; production cluster semantics remain future hardening. |
| GitOps / Argo CD | experimental | `internal/adapters/gitops/local`, `internal/adapters/executor/argocd/noop.go`, GitOps/Argo CLI | Planning/status/guarded sync foundations only; no production Argo CD automation or Git provider integration. |
| Artifact / OCI digest | partial | `internal/domain/artifact`, `internal/adapters/artifact/oci`, `internal/adapters/repository/postgres/artifact_registry_store.go`, `000011_policy_artifact_registry_catalog`, `/api/v1/artifacts`, `/api/v1/artifact-registries`, `nivora artifact` | OCI parsing, digest resolution foundation, release-bound artifact inventory, release binding lookup, registry metadata catalog, CredentialRef linkage, explicit insecure registry flag, and PostgreSQL-backed registry catalog persistence in configured server mode exist. Full Harbor/Nexus/JFrog management APIs and registry crawling are not implemented. |
| DevSecOps scanner | foundation | `internal/usecase/security`, `internal/adapters/security/noop`, `/api/v1/security/scans`, `/api/v1/security/findings`, `nivora security scans/findings` | Noop/fake scanner, built-in manifest checks, stored scan queries, finding filters, API routes, CLI query commands, and OpenAPI schemas exist. Trivy/Cosign/SBOM integrations remain future work. |
| Policy gates | foundation | `internal/usecase/security`, `internal/usecase/policy`, `internal/adapters/repository/postgres/policy_store.go`, `internal/ports/policy`, `000011_policy_artifact_registry_catalog`, `/api/v1/policies`, `/api/v1/policies/{id}/attachments`, `nivora policy` | Built-in simple rules, policy definition catalog, scope attachments, API/CLI CRUD, evaluation, and PostgreSQL-backed policy catalog/attachment persistence in configured server mode exist. No OPA/Kyverno/Gatekeeper integration. |
| Secret management | partial | `internal/ports/secret`, builtin/Vault/Kubernetes/KMS adapters | SecretRef model, redaction, and provider skeletons exist; production provider lifecycle is not validated. |
| Auth/RBAC | partial | `internal/usecase/auth`, auth middleware tests, table-driven RBAC tests, cross-tenant isolation tests, `docs/security/ROUTE_PERMISSION_MATRIX.md` | Dev/token/OIDC foundation, route permission matrix, route coverage tests, and cross-tenant isolation tests exist. Direct PipelineRun, DeploymentRun, ReleaseExecution, release, and visualization read routes now have explicit `project.read` coverage. Auth store has PostgreSQL persistence. Full enterprise SSO remains future work. |
| Org/project/application/environment catalog | foundation | `internal/usecase/catalog`, `internal/adapters/repository/postgres/catalog_store.go`, `000010_catalog_persistence`, `/api/v1/orgs`, `/api/v1/projects`, `/api/v1/applications`, `/api/v1/environments`, `nivora org/project/application/environment` | Foundation CRUD, parent validation, enabled/disabled state, RBAC-protected API routes, CLI commands, OpenAPI schemas, and PostgreSQL persistence in configured server mode exist. Full tenant lifecycle policy and cross-service provisioning remain future work. |
| Release target catalog | foundation | `internal/usecase/catalog`, `internal/adapters/repository/postgres/catalog_store.go`, `000010_catalog_persistence`, `/api/v1/release-targets`, `nivora target`, `examples/release-targets/` | Foundation target metadata CRUD, parent environment validation, enabled/disabled state, safety flags default false, validate endpoint, RBAC-protected API routes, CLI commands, and PostgreSQL persistence in configured server mode exist. Target execution still goes through DeploymentRun/ReleaseExecution foundations. |
| SCM repository catalog | foundation | `internal/usecase/catalog`, `internal/adapters/repository/postgres/catalog_store.go`, `000010_catalog_persistence`, `/api/v1/repositories`, `nivora repository`, `internal/ports/scm` | Foundation repository metadata CRUD, CredentialRef linkage, parent project validation, RBAC-protected API routes, CLI commands, OpenAPI schemas, and PostgreSQL persistence in configured server mode exist. Real GitHub/GitLab/Gitea network integration remains skeleton/future work. |
| Approval/change window | foundation | `internal/usecase/approval`, approval/change-window API routes, `POST /api/v1/approvals/{id}/resume-subject`, `internal/adapters/repository/postgres/approval_store.go`, `approval_store_test.go`, ReleaseExecution/DeploymentRun approval resume routes | Backend approval lifecycle, change-window evaluation, approval notification records, hash-chained approval audit, and guarded resume/stop paths for waiting DeploymentRun and ReleaseExecution exist. Postgres mode has opt-in recovery coverage for approval/change-window/notification/event/audit records. Pipeline subject resume, full ITSM/workflow integration, external notification delivery operations, and frontend approval UI remain out of scope. |
| Multi-cloud | placeholder | cloud domain, provider ports, fake/AWS/Aliyun/Tencent skeletons | Inventory skeleton/fake behavior only; no cloud deployment or complete provider inventory. |
| Host deployment | experimental | host target models, noop host executor, SSH skeleton | Plan/dry-run/noop foundation; remote execution is disabled by default and guarded. |
| Web console | experimental | `web/` Vite React app, `make verify-web` | Minimal UI foundation consuming backend APIs; not a production UI. |
| MCP control plane | foundation | `cmd/nivora-mcp`, `cmd/nivora mcp read-resource/call-tool`, `internal/api/mcp`, `internal/app/mcp`, `make verify-mcp`, `make verify-ai-control-plane`, `scripts/smoke-mcp-local.sh`, `scripts/validate-mcp-scenarios.sh`, `examples/mcp/scenarios/` (29 scenarios), `examples/mcp/golden-answers/` (29 answers), `internal/api/mcp/scenario_test.go`, `internal/api/mcp/server_test.go`, `internal/api/http/routes/tenant_isolation_test.go`, `docs/status/AI_CONTROL_PLANE_DEEP_AUDIT.md`, `docs/status/AI_OPERATOR_JOURNEYS.md`, `docs/status/MCP_ENTERPRISE_OPENING_DECISION.md`, `docs/status/MCP_ENTERPRISE_RISK_REGISTER.md`, `docs/security/MCP_PERMISSION_MATRIX.md`, `docs/security/MCP_TENANT_SCOPE_REVIEW.md`, `docs/security/mcp-threat-model.md`, `docs/rfcs/mcp-control-plane.md`, `docs/rfcs/remote-mcp-read-only.md` | Local stdio MCP exposes read-only resources for runtime recovery, aggregate events/logs with response caps, configured request timeouts, catalog summaries, pipeline definitions, deployments, releases, release-bound artifact inventory, security findings, policy result summaries, persisted evidence-bundle list/read access for `audit.read`, local CLI resource/tool inspection, plan-only tools with `mutated=false`, prompts that treat logs/events/manifests/audit messages as untrusted evidence, denied action tools, runner-token rejection, redaction, compliance-backed audit, scoped PipelineRun read/logs/timeline, scoped DeploymentRun read/health/diff, scoped ReleaseExecution read/timeline, project-scoped HTTP creation metadata for PipelineRun/DeploymentRun/ReleaseExecution, HTTP project-scope guards for PipelineRun/DeploymentRun/ReleaseExecution detail and visualization reads, HTTP evidence bundle scope checks for PipelineRun/DeploymentRun/ReleaseExecution subjects, HTTP aggregate events/logs/audit timeline filtering for PipelineRun/DeploymentRun/ReleaseExecution records, and aggregate MCP event/log filtering when project scope fields are present, plus validated golden operator scenarios. Remote read-only MCP is no-go until OAuth/OIDC or service-account auth, complete tenant filters for artifact/security/release evidence records, rate limits, pagination, remote timeout/cap proof, and remote audit tests exist. Action MCP remains blocked. |
| Integration capability index | foundation | `internal/usecase/integration`, `/api/v1/integrations`, `internal/api/http/routes/integrations_test.go` | Read-only adapter/plugin capability index labels skeleton, noop, foundation, and experimental entries. It does not configure or invoke external systems. |
| Plugin system | foundation | `internal/usecase/plugin`, `api/proto/plugin.proto`, plugin templates | Manifest/capability registry and protocol skeleton; no unsafe dynamic loading or marketplace. |
| Packaging | partial | Dockerfile, Docker Compose, Helm, Kubernetes manifests, `deployments/helm/values-production.yaml`, `deployments/docker-compose/docker-compose.production.example.yaml`, `scripts/smoke-production-install.sh`, `scripts/verify-helm-safety.sh` (11/11) | Production profiles validated statically via smoke-production-install. Helm safety 11/11. Compose enforces env placeholders for secrets. No live deployment smoke; no Kubernetes operator. |
| Observability | partial | request/correlation middleware, `/metrics`, diagnostics endpoints, filtered `/api/v1/audit-logs`, `/api/v1/events`, `/api/v1/logs`, `nivora audit/events/logs search`, MCP `nivora://events`, MCP `nivora://logs`, `nivora_search_events`, `nivora_search_logs`, `/api/v1/visualization`, visualization timelines | Lightweight metrics, diagnostics, filtered aggregate runtime events/logs/audit reads, CLI query commands, MCP read-only aggregate event/log search with response caps, visualization API discovery, and runbooks exist. Distributed tracing/export dashboards, production-scale log storage, and external log retention remain future work. |
| Audit/evidence | partial | `internal/usecase/compliance`, audit/evidence routes, `internal/adapters/repository/postgres/compliance_store.go`, `000008_compliance_audit_evidence`, `000009_governance_persistence`, `approval_store_test.go`, `internal/api/http/routes/tenant_isolation_test.go`, `nivora evidence list/generate/export` | SHA-256 hash-chained audit writes are wired into all 9 audit-producing stores (5 governance + 4 runtime). Evidence bundles can be generated, persisted, listed by subject, fetched by ID, and exported as JSON or Markdown without secret values. PipelineRun, DeploymentRun, and ReleaseExecution evidence bundles are now scope-checked for project-scoped HTTP auditors and persisted with scope metadata when verifiable. Release/artifact/security evidence still needs first-class ownership metadata. Approval audit-chain verification now has opt-in PostgreSQL coverage; full hash-chain verification still depends on the separate PostgreSQL integration job. |
| Persistence | partial | Runtime and catalog stores have Postgres implementations under `internal/adapters/repository/postgres/`, migrations 000001-000011, CI postgres-integration job | Core runtime stores plus org/project/application/environment/repository/release-target catalog metadata, Pipeline definitions, policy catalog/attachments, artifact registry catalog records, approval records, change windows, approval notifications, tenancy quota/usage summaries, and governance audit/event records have PostgreSQL persistence with runtime `WithConfig` wiring. Multi-process recovery e2e is not yet fully proven in baseline `make verify`. |
| Backup/restore | documented-only | `docs/operations/backup-restore.md`, HA/DR docs, optional smoke scripts | Procedures and optional smoke checks exist, but full restore drills are not automated or production-verified. |
| Production doctor | foundation | `internal/usecase/doctor`, `nivora doctor`, `docs/operations/production-doctor.md`, config safety tests | Read-only config posture checks for production-like installs, including runtime store, auth, unsafe flags, MCP action tools, audit/event-outbox persistence posture, runner identity config, and explicit `NOT_CHECKED` entries for live route/secret-scan/runner/audit checks. It does not prove production readiness and does not open live database, migration, runner, or audit-chain checks. |

## Contract Notes

- If placeholder route groups are reintroduced, they must return structured `not_implemented` responses and be labeled in OpenAPI.
- OpenAPI paths must match registered HTTP routes at route/path level.
- OpenAPI and AsyncAPI describe a hardened beta-candidate foundation, not a production-ready platform.
- External adapters labeled noop, fake, skeleton, placeholder, or experimental must not be described as complete integrations.
