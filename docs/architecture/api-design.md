# API Design

The HTTP API starts with health, readiness, version, and system info endpoints. Major resource groups are reserved under `/api/v1` and return clear `not_implemented` responses in Phase 0.

OpenAPI and AsyncAPI specs are maintained by hand for now. Generated code is intentionally not included in Phase 0.

