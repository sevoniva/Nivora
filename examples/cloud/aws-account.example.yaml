apiVersion: nivora.io/v1alpha1
kind: CloudAccount
metadata:
  name: aws-dev
spec:
  provider: aws
  credentialRef: credential-ref-placeholder
  config:
    provider: aws
    accountId: aws-account-placeholder
    defaultRegion: us-east-1
    regions:
      - us-east-1
      - us-west-2
    credentialRef: credential-ref-placeholder
