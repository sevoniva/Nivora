# ADR 0006: Use OpenAPI and AsyncAPI

## Decision

Nivora documents HTTP APIs with OpenAPI and event channels with AsyncAPI.

## Rationale

The project needs clear external contracts before generated clients or servers are introduced.

