apiVersion: nivora.io/plugin/v1alpha1
name: example-cloud-plugin
type: cloud
version: 0.1.0
protocol: http
endpoint: https://plugins.example.invalid/cloud
capabilities:
  - name: cloud.inventory
    description: Return cloud inventory metadata through a CredentialRef.
compatibility:
  pluginApiVersion: v1alpha1
  nivoraMinVersion: 0.1.0-alpha.1
  protocols:
    - http
lifecycle:
  health: true
  capabilities: true
  validateConfig: true
  execute: true
status: external
metadata:
  template: "true"
