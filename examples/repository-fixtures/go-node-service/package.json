{
  "name": "nivora-repository-fixture",
  "version": "0.0.0-example",
  "private": true,
  "scripts": {
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "example"
  },
  "devDependencies": {
    "vite": "example",
    "typescript": "example",
    "vitest": "example"
  }
}
