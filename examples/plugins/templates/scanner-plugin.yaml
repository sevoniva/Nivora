apiVersion: nivora.io/plugin/v1alpha1
name: example-scanner-plugin
type: scanner
version: 0.1.0
protocol: http
endpoint: https://plugins.example.invalid/scanner
capabilities:
  - name: security.scan
    description: Return structured findings for artifacts, manifests, or plans.
compatibility:
  pluginApiVersion: v1alpha1
  nivoraMinVersion: 0.1.0-alpha.1
  protocols:
    - http
lifecycle:
  health: true
  capabilities: true
  validateConfig: true
  execute: true
status: external
metadata:
  template: "true"
