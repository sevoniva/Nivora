apiVersion: nivora.io/v1alpha1
kind: Credential
metadata:
  name: example-ssh-credential
spec:
  type: ssh_key
  scopeType: environment
  scopeId: example-hosts
  secretRef:
    provider: builtin
    key: examples/ssh/private-key
  metadata:
    hostGroupRef: example-host-group
    note: placeholder only
