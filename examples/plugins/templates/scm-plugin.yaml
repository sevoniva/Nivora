apiVersion: nivora.io/plugin/v1alpha1
name: example-scm-plugin
type: scm
version: 0.1.0
protocol: http
endpoint: https://plugins.example.invalid/scm
capabilities:
  - name: scm.repository.inspect
    description: Inspect repository metadata through an external SCM adapter.
compatibility:
  pluginApiVersion: v1alpha1
  nivoraMinVersion: 0.1.0-alpha.1
  protocols:
    - http
lifecycle:
  health: true
  capabilities: true
  validateConfig: true
  execute: true
status: external
metadata:
  template: "true"
