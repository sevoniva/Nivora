# Nivora Capability Status

This status page is the public source of truth for implemented vs. partial vs. placeholder behavior. Historical audit reports in this directory are useful context, but this page should be treated as the current summary.

Current maturity: **hardened beta-candidate (0.9.0-rc.1), not production-ready**.

Nivora is **not production-ready**. All 11 core runtime stores have PostgreSQL implementations, audit hash chaining is wired through the audit-producing store paths, route/RBAC tests cover critical cases, runner isolation profiles and production config gates exist, and packaging smoke checks cover Helm and Docker Compose profiles. Remaining blockers include OS-level runner sandboxing, live install and restore drills at production scale, complete external adapter integrations, broader remote MCP controls, and final API/operation hardening. See `STORE_PERSISTENCE_MATRIX.md`, `ENTERPRISE_PRODUCTION_READINESS_REVIEW.md`, `ENTERPRISE_READINESS_MATRIX.md`, and the historical audit reports for evidence and limitations.

## Status Legend

| Status | Meaning |
|---|---|
| implemented | Implemented, tested, and verifiable for the documented local/foundation scope. |
| partial | Useful implementation exists, but important durability, safety, persistence, scale, or integration gaps remain. |
| foundation | Model, ports, adapters, examples, and tests exist to support future hardening, but the capability is not complete. |
| placeholder | Route, package, or document exists mainly to reserve shape and return structured not_implemented or skeleton behavior. |
| experimental | Available only for guarded evaluation or local validation; not production automation. |
| documented-only | Described in docs but not materially implemented. |
| missing | Not implemented and not usable. |

## Capability Matrix

| Capability | Status | Evidence | Notes / Limitations |
|---|---|---|---|
| PipelineRun runtime | implemented | `internal/usecase/pipeline`, `internal/adapters/executor/shell`, `scripts/smoke-pipelinerun-local.sh`, PipelineRun CLI server-token tests | Shell-based local/foundation runtime; server-backed PipelineRun read/log/cancel commands send bearer tokens from `--token-env`; not a full distributed workflow engine. |
| Pipeline definition catalog | partial | `internal/usecase/pipeline/catalog.go`, `internal/adapters/repository/postgres/pipeline_definition_store.go`, `000010_catalog_persistence`, `000012_pipeline_definition_versions`, `/api/v1/pipelines`, `/api/v1/pipelines/{id}/runs`, `/api/v1/pipelines/{id}/versions`, `/api/v1/pipelines/{id}/rollback`, `nivora pipeline definition`, `docs/dev/pipeline-definitions.md` | Validated Pipeline DSL definitions can be created, listed, updated with version/hash, fetched, disabled, run from the current or a saved historical catalog definition version, restored from an older saved version as a new current version, and persisted when `database.runtime_store: postgres` is configured. PipelineRuns created from the catalog keep `pipelineId` and `pipelineVersionId`. Scheduling policy remains future work. |
| DeploymentRun runtime | partial | `internal/usecase/deployment`, deployment API routes, smoke dry-run scripts, `internal/adapters/repository/postgres/deployment_store.go`, optional PostgreSQL recovery tests in `internal/adapters/repository/postgres/runtime_recovery_integration_test.go`, deployment CLI server-token tests | PostgreSQL persistence and restart-style repository recovery are covered when `NIVORA_RUN_POSTGRES_INTEGRATION=true`; server-backed deployment CLI plan/run/apply/read/rollback/resume/sync commands send bearer tokens from `--token-env`; saved security policy attachments can deny DeploymentRun pre-checks or require approval through the existing approval workflow; production worker orchestration and full CD hardening remain incomplete. |
| ReleaseExecution | partial | `internal/usecase/releaseorchestration`, release API routes, `POST /api/v1/releases/{id}/cancel`, `POST /api/v1/releases/executions/{execution_id}/cancel`, `POST /api/v1/releases/{id}/evidence`, `nivora release list`, `nivora release plan <id>`, `nivora release deploy <id>`, `nivora release cancel`, `nivora release execution cancel`, `nivora release evidence`, release CLI server-token tests, `make verify-release`, `internal/adapters/repository/postgres/release_orchestration_store.go`, optional PostgreSQL recovery tests in `internal/adapters/repository/postgres/runtime_recovery_integration_test.go`, runtime recovery center aggregation | Sequential orchestration, release-level cancel intent, filtered server-backed Release inventory by project, environment, application, and status, Release status lifecycle updates (`Ready`, `Planning`, `WaitingApproval`, `Deploying`, `Succeeded`, `Failed`, `Canceled`), saved security policy attachment gates for deny/require-approval decisions, status update events/audit, cascading cancellation for non-terminal ReleaseExecutions and linked non-terminal DeploymentRuns, server-backed Release ID planning/deploy CLI for safe noop/webhook targets, server-backed `--catalog-target` planning/deploy that resolves enabled ReleaseTarget catalog metadata, server-backed release CLI commands using `--token-env`, release evidence generation, PostgreSQL persistence, target state recovery, and stale execution queries exist; Release ID CLI mode does not support executable Kubernetes/Argo/host target specs without `--file`, catalog metadata alone cannot run those target types, cancel does not roll back deployments, delete resources, or mutate already-terminal ReleaseExecutions/DeploymentRuns, partially succeeded execution maps to release-level `Failed`, and automatic ReleaseExecution mutation in recovery remains manual/report-only. |
| Runner protocol | partial | runner routes in `internal/api/http/routes/routes.go`, PipelineStore runner methods, `runtime_runner_groups` migration, runner-token scope tests, runner token rotation/revoke route tests, project/environment-scoped claim tests, runner group claim tests, executor/capability normalization tests, unrelated job mutation negative tests, admin compatibility job mutation scope tests, PostgreSQL runner claim safety integration test, PostgreSQL runner token rotation/revoke recovery test, `nivora runner groups`, `nivora runner offline-detect`, runner admin CLI server-token tests | Token/heartbeat/claim/log/status/offline-detect foundations exist, runner-owned endpoints use runner tokens, route tests prove old tokens fail after rotation and revoked tokens cannot heartbeat, project- and environment-scoped runner labels plus RunnerGroup project/environment/executor/concurrency constraints limit job claim in memory/PostgreSQL/HTTP paths, PostgreSQL integration tests cover project/environment mismatch, executor mismatch, capability-based claim, runner max concurrency, RunnerGroup max concurrency, and restart persistence for rotated/revoked token denial before heartbeat, claim, log append, or status update mutation, runner registration normalizes executor/capability declarations before allow-list checks, project- and environment-scoped runner admin compatibility endpoints cannot append logs or update status for another scope's jobs, production config rejects `local-dev` and `shell-hardened` runner isolation profiles, and runner admin/group commands use API bearer tokens from `--token-env`; sandboxing and fleet-scale behavior are not production-grade. |
| Shell executor | implemented | `internal/adapters/executor/shell`, 18 shell executor tests | Enterprise controls: timeout clamp (3600s max), output truncation (10MB), workspace isolation with cleanup, sensitive env blocklist (20+ patterns), process group cleanup (Setpgid+SIGKILL), env isolation mode. NOT a container/VM sandbox; operators must still isolate untrusted workloads. |
| Kubernetes YAML apply | experimental | `internal/adapters/executor/yaml_apply`, guarded deployment apply command/API, `internal/usecase/deployment/k8s_safety.go`, Kubernetes safety policy and DeploymentRun execution-path tests, `examples/deployments/yaml-safety-policy.yaml` | Apply and rollback require explicit confirmation; no default destructive behavior; the default safety gate runs before dry-run/apply and rejects missing target namespace, Kubernetes system namespaces, cluster-scoped resources, manifest namespace mismatches, privileged containers, hostPath, host namespace modes, `:latest` images, manifests larger than 1 MiB, and more than 100 rendered resources. Deployment specs can tighten the policy with `spec.kubernetesSafety` namespace/kind allow/deny lists, stricter size/resource-count limits, and digest warnings; these options cannot disable built-in unsafe denials or relax default limits. Plan-only requests surface safety warnings. Production cluster semantics, live namespace locks, and advanced environment-specific policy storage remain future hardening. |
| GitOps / Argo CD | experimental | `internal/adapters/gitops/local`, `internal/adapters/executor/argocd/noop.go`, GitOps/Argo CLI, `/api/v1/deployments/gitops/plan`, `examples/deployments/argocd-plan-catalog.yaml` | Planning/status/guarded sync foundations exist. Server-backed GitOps plans can now resolve `target.repositoryId` from the SCM repository catalog into repository URL, default revision, provider, and name metadata without contacting the SCM provider, allowing inline repository credentials, or resolving CredentialRef values. No production Argo CD automation, provider clone/write integration, or default sync behavior is implemented. |
| Artifact / OCI digest | partial | `internal/domain/artifact`, `internal/adapters/artifact/oci`, `internal/adapters/repository/postgres/artifact_registry_store.go`, `internal/adapters/repository/postgres/release_store.go`, `000011_policy_artifact_registry_catalog`, `000013_runtime_artifact_catalog`, `/api/v1/artifacts`, `/api/v1/artifact-registries`, `/api/v1/artifact-registries/validate`, `/api/v1/artifact-registries/{id}/validate`, `/api/v1/artifact-registries/{id}/artifacts`, `nivora artifact`, `nivora release create --project-id`, `nivora release list`, artifact/release CLI server-token tests | OCI parsing, digest resolution foundation, explicit OCI repository tag listing through saved registry metadata, standalone artifact catalog tracking, release-bound artifact indexing, release binding lookup, explicit project ownership metadata on server-backed Release creation and bound artifacts, filtered Artifact inventory by project/environment/type/name/registry/repository/digest/reference through API/CLI/MCP, filtered Release inventory by project/environment/application/status, registry metadata catalog, shared direct/saved registry validation rules, inline credential rejection in registry endpoints, CredentialRef linkage, explicit insecure registry flag, server-backed artifact/registry/release CLI token handling, PostgreSQL-backed registry/artifact catalog persistence in configured server mode, HTTP scoped artifact catalog guards, and MCP scoped artifact/release-binding/event filtering exist. Full Harbor/Nexus/JFrog management APIs, CredentialRef value resolution during registry validation, and registry crawling are not implemented. |
| DevSecOps scanner | foundation | `internal/usecase/security`, `internal/adapters/security/noop`, `/api/v1/security/scans`, `/api/v1/security/findings`, `/api/v1/security/findings/{id}`, `nivora security scans/findings` | Noop/fake scanner, built-in manifest checks, stored scan queries, project/environment-aware scan and finding filters through API/CLI, finding detail lookup, API routes, CLI query commands, OpenAPI schemas, PostgreSQL project/environment scan scope columns, HTTP scoped list/detail guards, and MCP scoped security findings/policy/event filtering exist. Trivy/Cosign/SBOM integrations remain future work. |
| Policy gates | foundation | `internal/usecase/security`, `internal/usecase/policy`, `internal/adapters/repository/postgres/policy_store.go`, `internal/adapters/repository/postgres/security_store.go`, `internal/ports/policy`, `000011_policy_artifact_registry_catalog`, `000016_security_policy_results`, `/api/v1/policies`, `/api/v1/policies/evaluate`, `/api/v1/policies/{id}/evaluate`, `/api/v1/policies/results`, `/api/v1/policies/results/{id}`, `/api/v1/policies/{id}/attachments`, `nivora policy`, policy CLI server-token tests | Built-in simple rules, policy definition catalog, scope attachments, saved policy evaluation, stored policy evaluation result catalog with scoped project/environment list/detail queries through API/CLI, saved policy application during security scan creation through explicit `policyId` or environment/project/global attachment resolution, saved policy attachment gates for DeploymentRun and ReleaseExecution pre-checks, disabled-policy rejection, API/CLI CRUD, server-backed policy CLI token handling, and PostgreSQL-backed policy catalog/attachment/result persistence in configured server mode exist. No OPA/Kyverno/Gatekeeper integration. |
| Secret management | partial | `internal/ports/secret`, builtin/Vault/Kubernetes/KMS adapters, `nivora secret`, `nivora credential`, secret/credential CLI token-env and no-value output tests | SecretRef model, redaction, provider skeletons, server-backed secret/credential CLI token handling, provider validation status sanitization, and response safety tests exist. Production provider lifecycle and external secret storage are not validated. |
| Auth/RBAC | partial | `internal/usecase/auth`, auth middleware tests, table-driven RBAC tests, cross-tenant isolation tests, API token lifecycle route tests, `internal/adapters/repository/postgres/auth_store_test.go`, `docs/security/ROUTE_PERMISSION_MATRIX.md`, `nivora auth users`, `nivora auth roles`, `nivora org/project/environment members` | Dev/token/OIDC foundation, route permission matrix, route coverage tests, cross-tenant isolation tests, identity directory CLI reads, and org/project/environment membership list/add CLI commands exist. Direct PipelineRun, DeploymentRun, ReleaseExecution, release, and visualization read routes now have explicit `project.read` coverage. Auth store has PostgreSQL persistence, including API token create/rotate/revoke behavior with token hashes hidden from list/response paths, CLI token creation with RFC3339 `--expires-at`, route-level old-token rejection after rotation, revoked-token rejection, expired-token rejection, and rotation verified across a PostgreSQL pool restart. Full enterprise SSO remains future work. |
| Tenant quotas / usage | foundation | `internal/usecase/tenancy`, `/api/v1/tenancy/quota`, `/api/v1/tenancy/usage`, `nivora quota view/set`, `nivora usage summary`, PostgreSQL tenancy store wiring, quota/usage CLI server-token tests | Quota metadata can be viewed and updated by scope, usage summaries can be read, server-backed quota/usage CLI commands use `--token-env`, and configured PostgreSQL runtime mode can persist quota metadata. Distributed rate limiting, real billing, and production tenant provisioning remain future work. |
| Org/project/application/environment catalog | foundation | `internal/usecase/catalog`, `internal/adapters/repository/postgres/catalog_store.go`, `000010_catalog_persistence`, `/api/v1/orgs`, `/api/v1/projects`, `/api/v1/applications`, `/api/v1/environments`, `nivora org/project/application/environment` | Foundation CRUD, parent validation, enabled/disabled state, RBAC-protected API routes, CLI commands, OpenAPI schemas, and PostgreSQL persistence in configured server mode exist. Full tenant lifecycle policy and cross-service provisioning remain future work. |
| Release target catalog | foundation | `internal/usecase/catalog`, `internal/adapters/repository/postgres/catalog_store.go`, `000010_catalog_persistence`, `/api/v1/release-targets`, `nivora target`, `examples/release-targets/` | Foundation target metadata CRUD, parent environment validation, enabled/disabled state, safety flags default false, validate endpoint, RBAC-protected API routes, CLI commands, PostgreSQL persistence in configured server mode, and ReleasePlan `targetId` resolution exist. Safe noop/webhook catalog targets can be planned from Release ID mode. Executable target types still need explicit Deployment specs and guarded runtime flags; target execution goes through DeploymentRun/ReleaseExecution foundations. |
| SCM repository catalog | foundation | `internal/usecase/catalog`, `internal/adapters/repository/postgres/catalog_store.go`, `000010_catalog_persistence`, `/api/v1/repositories`, `/api/v1/repositories/{id}/validate`, `nivora repository validate`, `internal/ports/scm`, GitOps deployment planning via `target.repositoryId` | Foundation repository metadata CRUD, metadata-only validation, inline credential rejection for repository URLs, CredentialRef linkage without value resolution, parent project validation, RBAC-protected API routes, CLI commands, OpenAPI schemas, PostgreSQL persistence in configured server mode, and GitOps plan metadata resolution exist. Real GitHub/GitLab/Gitea network integration, credential use, cloning, push, and provider write operations remain skeleton/future work. |
| Approval/change window | foundation | `internal/usecase/approval`, approval/change-window API routes, `nivora approvals create/list/get/approve/reject/cancel/expire/resume`, `POST /api/v1/approvals/{id}/resume-subject`, `nivora change-window create/list/get/evaluate`, `GET /api/v1/notifications`, `nivora notification list/test`, governance CLI token-env tests, `internal/adapters/repository/postgres/approval_store.go`, `approval_store_test.go`, PipelineRun/ReleaseExecution/DeploymentRun approval resume routes | Backend approval lifecycle, approval request create/read/decision CLI, change-window create/list/get/evaluate, approval request and terminal decision notification records, hash-chained approval audit, server-backed governance CLI token handling, CLI/API notification record listing, and guarded resume/stop paths for waiting DeploymentRun, ReleaseExecution, and Paused PipelineRun subjects exist. Pipeline approval resume queues the run and does not execute shell steps directly. Decision notifications are metadata-only and do not duplicate approver comments. Postgres mode has opt-in recovery coverage for approval/change-window/notification/event/audit records. Full ITSM/workflow integration, external notification delivery operations, and frontend approval UI remain out of scope. |
| Multi-cloud | foundation | cloud domain, provider ports, fake/AWS/Aliyun/Tencent skeletons, `/api/v1/cloud/accounts`, `nivora cloud account create/list/get/validate`, `nivora cloud inventory/clusters/hosts/registries`, cloud CLI token-env tests | Account metadata can be created, listed, fetched, validated, and used for deterministic fake/provider-skeleton inventory. Server-backed cloud CLI commands send bearer tokens from `--token-env`, while local provider metadata remains available through `--local`. Credentials are represented by CredentialRef metadata only. No real cloud deployment, full provider SDK inventory, pagination, permission discovery, or cloud resource mutation. |
| Host deployment | experimental | host target models, noop host executor, SSH skeleton, `POST /api/v1/deployments/host/plan`, `/api/v1/host-groups`, `nivora host-groups list/create/get`, server-backed dry-run/noop host run through `POST /api/v1/deployments`, `nivora deployment host run --local=false` | Host group metadata can be created, listed, and fetched through API/CLI without secret values or remote execution. Plan/dry-run/noop foundation works locally and through the server-backed DeploymentRun path. Remote execution is disabled by default and guarded; the CLI rejects server-backed host runs that request remote apply. |
| Web console | experimental | `web/` Vite React app, global API connection diagnostics, retryable request states, `make verify-web` | Minimal UI foundation consuming backend APIs. It checks `/api/v1/version` before loading runtime pages, shows a single backend-unavailable state when the API cannot be reached, and keeps per-view errors retryable. It is not a production UI and does not add backend behavior. |
| MCP control plane | foundation | `cmd/nivora-mcp`, `cmd/nivora mcp read-resource/call-tool`, `POST /api/v1/mcp/rpc`, `internal/api/mcp`, `internal/app/mcp`, `make verify-mcp`, `make verify-ai-control-plane`, `scripts/smoke-mcp-local.sh`, `scripts/validate-mcp-scenarios.sh`, `examples/mcp/scenarios/` (31 scenarios), `examples/mcp/golden-answers/` (31 answers), `internal/api/mcp/scenario_test.go`, `internal/api/mcp/server_test.go`, `internal/api/http/routes/mcp_remote_test.go`, `internal/api/http/routes/tenant_isolation_test.go`, `internal/api/http/routes/security_test.go`, `internal/api/http/routes/artifacts_test.go`, `docs/status/AI_CONTROL_PLANE_DEEP_AUDIT.md`, `docs/status/AI_OPERATOR_JOURNEYS.md`, `docs/status/MCP_ENTERPRISE_OPENING_DECISION.md`, `docs/status/MCP_ENTERPRISE_RISK_REGISTER.md`, `docs/security/MCP_PERMISSION_MATRIX.md`, `docs/security/MCP_TENANT_SCOPE_REVIEW.md`, `docs/security/mcp-threat-model.md`, `docs/rfcs/mcp-control-plane.md`, `docs/rfcs/remote-mcp-read-only.md` | Local stdio MCP exposes read-only resources for runtime recovery, aggregate events/logs with response caps, JSON-RPC request body caps, JSON-RPC transport-wide response caps, configured request timeouts, local JSON-RPC request rate limits, catalog summaries, paginated pipeline definition lists, deployments, filtered release inventory, release-bound artifact inventory, security findings, policy result summaries, stored PolicyResult lists with project/environment/decision filters, persisted evidence-bundle list/read access for `audit.read`, local CLI resource/tool inspection, plan-only tools with `mutated=false`, prompts that treat logs/events/manifests/audit messages as untrusted evidence, denied action tools, runner-token rejection, redaction, compliance-backed audit with Postgres MCP hash-chain integration proof, scoped PipelineDefinition list/read filtering with limit/offset pagination, scoped PipelineRun read/logs/timeline, scoped DeploymentRun read/health/diff, scoped ReleaseExecution read/timeline, scoped Release inventory filtering with limit/offset pagination, scoped Artifact inventory filtering with project/environment/type/name/registry/repository/digest/reference filters and limit/offset pagination, scoped artifact release-binding/event filtering, scoped SecurityScan findings/policy/event filtering, scoped PolicyResult list filtering with requested-project override and pagination, scoped runner summary filtering by runner scope labels, scoped capability-status warning metadata, project-scoped HTTP creation metadata for PipelineRun/DeploymentRun/ReleaseExecution/Artifact/SecurityScan/Runner, HTTP project-scope guards for PipelineRun/DeploymentRun/ReleaseExecution/Artifact/SecurityScan/Runner detail and visualization reads where applicable, HTTP evidence bundle scope checks for PipelineRun/DeploymentRun/ReleaseExecution subjects, HTTP aggregate events/logs/audit timeline filtering for PipelineRun/DeploymentRun/ReleaseExecution records, and aggregate MCP event/log filtering when project scope fields are present. Experimental remote read-only JSON-RPC exists behind `mcp.enabled=true` and `mcp.mode=http`; it requires bearer/service-account/OIDC auth, rejects anonymous/dev/runner-token use, enforces body/response caps and in-process per-subject rate limits, returns structured errors for unknown methods/resources/tools, supports limit/offset pagination on list tools and list-like resource URIs, records remote audit attribution for allowed calls and auth-boundary denials with request id, correlation id, MCP client id, transport, and remote address redacted where needed, and keeps action tools blocked. Broader remote MCP exposure still needs distributed rate limits for multi-replica deployments, operator deployment guidance, and more tenant-scope proof for future resources. Action MCP remains blocked. |
| Integration capability index | foundation | `internal/usecase/integration`, `/api/v1/integrations`, `internal/api/http/routes/integrations_test.go`, `nivora integrations list` | Read-only adapter/plugin capability index labels skeleton, noop, foundation, and experimental entries through API and CLI. It does not configure or invoke external systems. |
| Plugin system | foundation | `internal/usecase/plugin`, `api/proto/plugin.proto`, plugin templates, `/api/v1/plugins/{name}/capabilities`, `nivora plugins capabilities`, plugin CLI server-token tests | Manifest/capability registry, capability lookup through API/CLI, server-backed plugin CLI token handling, and protocol skeleton exist; no unsafe dynamic loading or marketplace. |
| Packaging | partial | Dockerfile, Docker Compose, Helm, Kubernetes manifests, `deployments/helm/values-production.yaml`, `deployments/docker-compose/docker-compose.production.example.yaml`, `scripts/smoke-production-install.sh`, `scripts/verify-helm-safety.sh` (13/13) | Production profiles validated statically via smoke-production-install. Helm safety 13/13. Compose enforces env placeholders for secrets. No live Kubernetes deployment smoke; no Kubernetes operator. |
| Observability | partial | request/correlation middleware, `/metrics`, diagnostics endpoints, `nivora system info/runtime/diagnostics`, filtered `/api/v1/audit-logs`, `/api/v1/events`, `/api/v1/logs`, `/api/v1/timeline`, `nivora audit/events/logs/timeline search`, MCP `nivora://events`, MCP `nivora://logs`, `nivora_search_events`, `nivora_search_logs`, `/api/v1/visualization`, visualization timelines | Lightweight metrics, diagnostics, system metadata CLI, filtered aggregate runtime events/logs/audit reads, aggregate event+log timeline queries with redaction and pagination, CLI query commands, MCP read-only aggregate event/log search with response caps, visualization API discovery, and runbooks exist. Distributed tracing/export dashboards, production-scale log storage, and external log retention remain future work. |
| Audit/evidence | partial | `internal/usecase/compliance`, audit/evidence routes, `internal/adapters/repository/postgres/compliance_store.go`, `000008_compliance_audit_evidence`, `000009_governance_persistence`, `approval_store_test.go`, `internal/usecase/compliance/service_test.go`, `internal/api/http/routes/tenant_isolation_test.go`, `nivora evidence list/generate/export`, `nivora retention-policy get/set/run`, audit/evidence CLI server-token tests | SHA-256 hash-chained audit writes are wired into all 9 audit-producing stores (5 governance + 4 runtime). Hash computation normalizes timestamps to PostgreSQL microsecond precision, chain verification follows `previous_hash` links instead of guessing order from timestamps, and an integration test covers same-microsecond burst writes. Evidence bundles can be generated, persisted, listed by subject, fetched by ID, and exported as JSON or Markdown without secret values; server-backed audit/evidence CLI commands use `--token-env`. Release evidence follows ReleaseExecution records and includes release plans, target DeploymentRun summaries, DeploymentPlans, policy results, approval gates, security findings, events, audits, log references, and deterministic bundle digests over redacted content. Retention policy metadata can be read and updated through API and CLI; retention runs can preview candidates and, with explicit confirmation, delete expired evidence bundles for the selected scope. Audit remains immutable and log/event cleanup is preview-only in this foundation. PipelineRun, DeploymentRun, ReleaseExecution, Release, Artifact, and SecurityScan evidence bundles are scope-checked for project-scoped HTTP auditors when subject ownership can be verified, and persisted bundles retain scope metadata. Full external evidence collection remains future work. |
| Persistence | partial | Runtime and catalog stores have Postgres implementations under `internal/adapters/repository/postgres/`, migrations 000001-000014, CI postgres-integration job | Core runtime stores plus org/project/application/environment/repository/release-target catalog metadata, Pipeline definitions and saved Pipeline definition version bodies, policy catalog/attachments, artifact registry catalog records, standalone and release-bound artifact catalog records, approval records, change windows, approval notifications, security scan project/environment scope, tenancy quota/usage summaries, and governance audit/event records have PostgreSQL persistence with runtime `WithConfig` wiring. Multi-process recovery e2e is not yet fully proven in baseline `make verify`. |
| Backup/restore | foundation | `docs/operations/backup-restore.md`, HA/DR docs, `scripts/smoke-backup-restore-postgres.sh`, `scripts/drill-backup-restore-postgres.sh`, GitHub `postgres-integration` job | PostgreSQL backup/restore smoke runs in the Postgres integration job, redacts database URLs, creates a clean source database, writes a PipelineRun, credential metadata, and a persisted evidence bundle through a Postgres-backed server, runs `pg_dump`, restores the dump into a separate temporary database, starts a server against the restored database, and verifies PipelineRun, evidence bundle, credential metadata, restored hash-chained audit records, and the credential audit hash chain. In CI the isolated-source and actual-restore paths are required. Full production restore drills, object-store restore at scale, encrypted backup storage, and operator-run live disaster recovery remain unproven. |
| Production doctor | foundation | `internal/usecase/doctor`, `nivora doctor`, `nivora doctor live`, `docs/operations/production-doctor.md`, config safety tests, live doctor CLI tests | Read-only config posture checks for production-like installs, including runtime store, auth, unsafe flags, MCP action tools, audit/event-outbox persistence posture, runner identity config, and explicit `NOT_CHECKED` entries for checks that remain external. The `live` mode calls existing server diagnostics, runtime recovery, and audit verify endpoints to summarize runtime mode, dependency degradation, event outbox counts, and audit hash-chain validity without mutating state. It does not prove production readiness, run migrations, reconcile runtime work, or replace backup/restore drills. |

## Contract Notes

- If placeholder route groups are reintroduced, they must return structured `not_implemented` responses and be labeled in OpenAPI.
- OpenAPI paths must match registered HTTP routes at route/path level.
- OpenAPI and AsyncAPI describe a hardened beta-candidate foundation, not a production-ready platform.
- External adapters labeled noop, fake, skeleton, placeholder, or experimental must not be described as complete integrations.
