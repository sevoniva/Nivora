apiVersion: nivora.io/v1alpha1
kind: ReleaseTarget
metadata:
  name: audit-only
spec:
  projectId: example-project
  environmentId: example-dev
  targetType: noop
  allowApply: false
  allowSync: false
  allowRemoteHostDeploy: false
  enabled: true
  labels:
    purpose: audit
