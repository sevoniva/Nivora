apiVersion: nivora.io/v1alpha1
kind: Credential
metadata:
  name: example-registry-credential
spec:
  type: registry
  scopeType: project
  scopeId: example-project
  secretRef:
    provider: builtin
    key: examples/registry/token
  metadata:
    registry: registry.example.invalid
    note: placeholder only
