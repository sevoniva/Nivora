# Deployment Model

Nivora treats deployment as a workflow from release artifact to target environment. GitOps is supported as one deployment mode, but host release, YAML apply, Helm, webhook, and cloud-native modes can coexist.

Phase 0 defines deployment run statuses and migration tables only. No production deployment logic is implemented.

